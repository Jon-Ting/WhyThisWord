// Future semantic-neighbour retrieval via embeddings. Currently unwired.
export async function findNeighbours(_lemma: string): Promise<string[]> {
  throw new Error("Embedding-based neighbour lookup not wired yet.");
}
